{
  "name": "night-api",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "9.0.0"
  },
  "license": "ISC",
  "scripts": {
    "start": "cd night-api && yarn && npm run serve",
    "heroku-postbuild": "cd night-react/ && yarn --production=false && yarn run build"
  },
  "dependencies": {  
    "express": "^4.16.2"    
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "nodemon": "^1.12.1"
  }
}
